#pragma once

enum Terrain { Plain, River, Mountain };
enum DinoType { Tyrannosaur, Brachiosaurus, Triceratops };
enum DinoPerkType { STAT_STR, STAT_DEX, STAT_INT };

enum GameState { InMenu, SelectingDinos, Showcase, InShop, Battle, BattleResult };